export default function Settings() {
  return (
    <section className="grid gap-6">
      <div className="card">
        <h2 className="text-2xl font-bold mb-2">Settings</h2>
        <p className="text-[var(--muted)]">
          Feature flags, API keys, and environment toggles will live here.
        </p>
      </div>
    </section>
  );
}
